<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sany.platform.mapper.SysUserMapper">
    <resultMap id="BaseResultMap" type="com.sany.platform.model.entity.SysUser">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="real_name" property="realName"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="user_type" property="userType"/>
        <result column="status" property="status"/>
        <result column="deleted" property="deleted"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>
    <sql id="BaseColumns">id, username, password, real_name, phone, email, user_type, status, deleted, create_time, update_time</sql>
    <select id="selectByUsername" resultMap="BaseResultMap">SELECT <include refid="BaseColumns"/> FROM tbl_sys_user WHERE username = #{username} AND deleted = 0</select>
    <select id="selectById" resultMap="BaseResultMap">SELECT <include refid="BaseColumns"/> FROM tbl_sys_user WHERE id = #{id} AND deleted = 0</select>
    <update id="updateById">UPDATE tbl_sys_user <set>update_time = NOW()<if test="realName!=null">,real_name=#{realName}</if><if test="phone!=null">,phone=#{phone}</if><if test="email!=null">,email=#{email}</if></set> WHERE id=#{id} AND deleted=0</update>
</mapper>
